import { NoSelectDirective } from './no-select.directive';

describe('NoSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new NoSelectDirective();
    expect(directive).toBeTruthy();
  });
});
